<div class="page-header">
  <h1><%= @admin.usuario&.nome %></h1>
  <div class="actions">
    <%= link_to "Editar", edit_admin_path(@admin), class: "btn btn-primary" %>
    <%= link_to "Voltar", admins_path, class: "btn btn-secondary" %>
  </div>
</div>
<div class="card"><%= render @admin %></div>