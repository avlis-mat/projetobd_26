<div class="detail-row"><span class="detail-label">Nome:</span>        <%= modelo.nome %></div>
<div class="detail-row"><span class="detail-label">Descrição:</span>   <%= modelo.descricao %></div>
<div class="detail-row"><span class="detail-label">Versão:</span>      <%= modelo.versao %></div>
<div class="detail-row"><span class="detail-label">Status:</span>      <%= modelo.status %></div>
<div class="detail-row"><span class="detail-label">Criado por:</span>  <%= modelo.usuario&.nome %></div>
<div class="detail-row"><span class="detail-label">Criado em:</span>   <%= modelo.criado_em&.strftime("%d/%m/%Y") %></div>